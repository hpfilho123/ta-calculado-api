describe('Minha função', () => {
  it('deve retornar verdadeiro quando chamado com argumentos válidos', () => {
    expect(1).toBe(1);
  });
});

describe('Minha função 2', () => {
  it('deve retornar verdadeiro quando chamado com argumentos válidos', () => {
    expect(1).toBe(1);
  });
});

describe('Minha função4', () => {
  it('deve retornar verdadeiro quando chamado com argumentos válidos', () => {
    expect(1).toBe(1);
  });
});
